# USTB RG Helper — 使用说明

## 简介

北京科技大学锐格实验平台（`ucb.ustb.edu.cn`）辅助工具。提供一键答题、批量答题、强制提交、显示答案、解除复制限制等功能。支持选择题、判断题、填空题、编程题四种题型。

## 安装

1. 安装 [Tampermonkey](https://www.tampermonkey.net/) 浏览器扩展
2. 打开 Tampermonkey → 管理面板 → 实用工具 → 导入
3. 选择 `USTB-RG-Helper.user.js` 文件，确认安装
4. 打开锐格平台任意题目页面，页面底部显示"已启用 USTB RG Helper"即安装成功

## 按钮说明

进入任意题目页面后，页面会显示以下五个按钮：

| 按钮 | 功能 | 适用场景 |
|------|------|----------|
| **一键答题** | 自动获取当前题目的参考答案，填入表单并提交 | 单题快速作答 |
| **批量答题** | 自动遍历**当前章节**的所有未完成题目，逐题回答并提交 | 完成某一章的全部题目 |
| **全局答题** | 展开所有章节，自动遍历**整个课程**的所有未完成题目，跨章节自动切换 | 一次性清空所有未做题目 |
| **强制显示答案** | 显示当前题目的参考答案，支持一键复制 | 查看答案但不提交 |
| **强制提交** | 绕过截止时间限制直接提交 | 补交过期作业 |

## 使用流程

### 日常使用

1. 打开锐格平台，进入练习页面
2. 在左侧树中点击要做的章节
3. 点击 **批量答题** 按钮，自动完成该章节所有题目
4. 页面左下角"需人工处理"面板显示需要手动处理的题目（如代码拼接题）

### 考前突击

1. 在任意题目页面点击 **全局答题**
2. 脚本自动展开所有章节，遍历全部未完成题目
3. 等待完成后，查看左下角面板中需要人工处理的题目
4. 手动处理完成后，可再次点击全局答题清理剩余题目

### 查看答案

1. 点击 **强制显示答案** 按钮
2. 答案以蓝色背景区域显示
3. 点击"复制"按钮可将答案复制到剪贴板
4. 选择题/判断题：显示 `A 正确` 等信息
5. 填空题：显示空内应填内容
6. 编程题：显示参考代码

### 补交过期作业

1. 进入已过期但仍想提交的题目
2. 点击 **强制提交** 按钮，不检查截止时间直接提交

## 支持的题型

| 题型 | 自动答题方式 | 备注 |
|------|-------------|------|
| 选择题（radio） | 正则匹配 `X、正确.` 定位正确选项，自动选中并提交 | 包括单选和判断 |
| 多选题（checkbox） | 同上，支持多选 | 较少见 |
| 填空题 | 从答案 HTML 提取纯文本，填入 ACE 编辑器或 input | **注意**：部分填空题也使用 ACE 编辑器，脚本已适配 |
| 编程题 | 从 `init_obj.content` 提取参考代码，解码 HTML 实体后填入 ACE 编辑器 | **代码拼接题**（含 `//start`/`//end` 标记）需人工处理 |

## 需人工处理的面板

页面左下角浮动面板实时显示需要手动处理的题目：

- **提交反馈非正确**：提交后服务器返回结果不是"回答正确"
- **未确认提交结果**：提交后未检测到反馈信息
- 最多显示最近 20 条记录
- 可在列表中找到对应题号，手动进入该题重新作答

## 常见问题

### Q: 批量答题突然停了？
A: 查看页面底部状态栏日志。可能是到了章节末尾、遇到无法自动处理的题目、或所有题目已完成。

### Q: 为什么会跳过一些题目？
A: 已完成的题目（显示"回答正确"或得分 1）会被自动跳过。之前尝试失败加入人工队列的题目也会被跳过。

### Q: 全局答题能跨教学方案吗？
A: 当前版本仅处理当前教学方案（如"课后作业"或"上机实验"）下的所有章节。如需跨方案，请在页面切换到另一个方案后再点全局答题。

### Q: 编程题代码编译错误？
A: 可能是代码拼接题。这类题编辑器中有 `//start`/`//end` 标记，参考答案是完整代码而非仅填充部分。请手动处理。

### Q: 如何停止批量答题？
A: 再次点击"批量答题"或"全局答题"按钮（按钮文字变为"停止"时表示正在运行中），或刷新页面。

## 按钮位置

按钮出现在题目表单区域（`#exercise_submit` 内），位于题目选项/输入框下方。

## 免责声明

本工具仅供学习参考，请勿用于违反学校规定的行为。使用本工具产生的任何后果由使用者自行承担。
